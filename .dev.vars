# 运行时变量，由 Wrangler / Cloudflare 注入 Worker。
# 本地复制为 .dev.vars；生产在 Worker「设置 → 变量和机密」中填写。

# --- 必填 ---
BETTER_AUTH_SECRET=rcbYMF1IgixMkRtkx5FJlD0PvnoBOjKw
BETTER_AUTH_URL=http://blog.20130825.xyz
GITHUB_CLIENT_ID=Ov23liRrDZ8ywI0zsCKt
GITHUB_CLIENT_SECRET=7c6dfe046b85801f2cf12b0c2507a7258bc95f19
DOMAIN=blog.20130825.xyz

# 仅本地开发使用。设为 dev 时不会真正发信，验证链接打在控制台。
# 生产环境不要设置，或设为 prod。
ENVIRONMENT=prod

# --- 可选：Umami ---
# 客户端埋点还需要构建时变量 VITE_UMAMI_WEBSITE_ID，website id 两边填同一个。
# UMAMI_WEBSITE_ID=your-umami-website-id
# UMAMI_SRC=https://cloud.umami.is
# UMAMI_API_URL=https://api.umami.is/v1
# Cloud 用 API Key；自托管用用户名密码，不要和 API Key 同时配。
# UMAMI_API_KEY=your-umami-cloud-api-key
# UMAMI_USERNAME=your-umami-username
# UMAMI_PASSWORD=your-umami-password

# --- 可选：Turnstile ---
# 还需要构建时变量 VITE_TURNSTILE_SITE_KEY。
# 测试 key：https://developers.cloudflare.com/turnstile/troubleshooting/testing/
# TURNSTILE_SECRET_KEY=your-turnstile-secret-key

# --- 可选：GitHub API（版本更新检查） ---
# 未认证请求限流 60 次/小时/IP，Workers 共享 IP 容易触发。
# 创建无权限的 Fine-grained token: https://github.com/settings/personal-access-tokens
# GITHUB_TOKEN=your-github-token
